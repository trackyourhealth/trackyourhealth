model Instrument {
  ...id

  study Study @relation(fields: [studyId], references: [id])
  studyId String

  name String
  title Json @default("{}")
  description Json @default("{}")

  content Json
  evaluations Json @default("{}")
  state Json @default("{}")

  duration Json?
  schedule Json?

  @@map("instruments")
}