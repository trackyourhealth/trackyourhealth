import { apiInstrumentData } from './api-instrument-data';

describe('apiInstrumentData', () => {
  it('should work', () => {
    expect(apiInstrumentData()).toEqual('api-instrument-data');
  });
});
